// import React, { useState } from 'react'
import React from 'react'
const Count2 = (props) => {

    return (
        <>
            <button onClick={ props.sahil } >increment</button>  <br /><br />

            <button onClick={ props.sutariya }>decrement</button>
        </>
    )
}

export default Count2
